{
  "name": "whatsapp_send_message",
  "description": "Send a WhatsApp message via Twilio API. Use this when you need to send a WhatsApp message to a phone number, reply to a customer, or notify someone via WhatsApp.",
  "parameters": {
    "type": "object",
    "properties": {
      "target_number": {
        "type": "string",
        "description": "Recipient's WhatsApp number with 'whatsapp:' prefix (e.g., 'whatsapp:+923001234567')"
      },
      "message_text": {
        "type": "string",
        "description": "The message content to send"
      },
      "vault_path": {
        "type": "string",
        "description": "Path to AI Employee Vault for logging (default: 'AI_Employee_Vault')",
        "default": "AI_Employee_Vault"
      }
    },
    "required": ["target_number", "message_text"]
  }
}
